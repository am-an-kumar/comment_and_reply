import styled from 'styled-components'

const TextArea = styled.textarea`
    padding: 0.4rem 1rem;
    border-radius: 2px;
    border: none;
    outline: none;
    resize: vertical;
`

export { TextArea }
